name: variables

on: 
  workflow_dispatch:

jobs:
  print-variables:
    runs-on: ubuntu-latest
    env:
      owner: "divya"
    steps:
      - name: Print variable values
        run: |
          echo "This is ${{ env.owner }}"     
          echo "You can add more steps to utilize variables as needed."
    
